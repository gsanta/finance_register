<h1>Products</h1>

<div class="list-group">
	{{#each model}}
	<div class="list-group-item active">
		<div class="list-group-item-text">
			<span>{{this.name}}</span>
			<span>{{this.price}}</span>
			<span>{{this.amount}}</span>
			<span>{{#link-to 'product' this}}Edit{{/link-to}}</span>
		</div> 
	</div>
	{{/each}}
</div>

{{outlet}}
